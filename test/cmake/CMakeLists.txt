cmake_minimum_required(VERSION 3.12)

set(TEST_FIND_MODULE_DIR "${CMAKE_BINARY_DIR}/CMakeModule/find")

file(COPY "${CMAKE_CURRENT_SOURCE_DIR}/find/CMakeLists.txt" DESTINATION ${TEST_FIND_MODULE_DIR})
file(COPY "${CMAKE_SOURCE_DIR}/cmake" DESTINATION ${TEST_FIND_MODULE_DIR})
